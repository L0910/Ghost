# This directory is used for Ghost runtime installation.
# Run `ghost install local` in this directory to set up the local Ghost instance.
# Runtime data (database, uploads, logs) is not committed to Git.
